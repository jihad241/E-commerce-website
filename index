<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FC ID Hub - Buy & Sell Football IDs</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <style>  // It is used for css.
        :root {
            --primary: #22c55e;
        }
        .dark {
            background-color: #0a0a0a;
            color: #f1f1f1;
        }
        .card-hover {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .card-hover:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
        }
        .nav-link {
            position: relative;
        }
        .nav-link:after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -2px;
            left: 0;
            background-color: #22c55e;
            transition: width 0.3s;
        }
        .nav-link:hover:after {
            width: 100%;
        }
    </style>
</head>
<body class="dark bg-zinc-950 text-zinc-100 font-sans">

<!-- Navbar -->
<nav class="bg-zinc-900 border-b border-zinc-800 sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
        <div class="flex items-center gap-8">
            <div class="flex items-center gap-2">
                <i class="fas fa-futbol text-green-500 text-3xl"></i>
                <span class="text-2xl font-bold tracking-tight">FC ID <span class="text-green-500">HUB</span></span>
            </div>
            
            <div class="hidden md:flex items-center gap-6 text-sm">
                <a href="index.php" class="nav-link text-white hover:text-green-400">Home</a>
                <a href="shop.php" class="nav-link text-white hover:text-green-400">Shop</a>
                <a href="#" class="nav-link text-white hover:text-green-400">Sell ID</a>
                <a href="#" class="nav-link text-white hover:text-green-400">Categories</a>
            </div>
        </div>

        <div class="flex items-center gap-4">
            <div onclick="toggleTheme()" class="w-9 h-9 flex items-center justify-center rounded-xl bg-zinc-800 hover:bg-zinc-700 cursor-pointer">
                <i id="theme-icon" class="fas fa-moon"></i>
            </div>
            
            <a href="cart.php" class="relative w-9 h-9 flex items-center justify-center rounded-xl bg-zinc-800 hover:bg-zinc-700">
                <i class="fas fa-shopping-cart"></i>
                <span id="cart-count" class="absolute -top-1 -right-1 bg-green-500 text-xs w-5 h-5 flex items-center justify-center rounded-full">0</span>
            </a>

            <?php if(isset($_SESSION['user_id'])): ?>
                <div class="flex items-center gap-3">
                    <span class="text-sm"><?php echo $_SESSION['username']; ?></span>
                    <a href="logout.php" class="px-4 py-2 bg-red-600 hover:bg-red-700 rounded-xl text-sm">Logout</a>
                </div>
            <?php else: ?>
                <a href="login.php" class="px-6 py-2.5 bg-zinc-800 hover:bg-zinc-700 rounded-2xl text-sm font-medium">Login</a>
                <a href="register.php" class="px-6 py-2.5 bg-green-600 hover:bg-green-500 rounded-2xl text-sm font-medium">Register</a>
            <?php endif; ?>
        </div>
    </div>
</nav>

<!-- Hero -->
<section class="relative h-screen flex items-center justify-center overflow-hidden">
    <div class="absolute inset-0 bg-[url('https://picsum.photos/id/1015/2000/1200')] bg-cover bg-center opacity-30"></div>
    <div class="absolute inset-0 bg-gradient-to-b from-transparent via-zinc-950/80 to-zinc-950"></div>
    
    <div class="relative max-w-4xl mx-auto text-center px-6">
        <h1 class="text-6xl md:text-7xl font-bold mb-6 leading-tight">
            Buy & Sell <span class="text-green-500">FC Mobile IDs</span>
        </h1>
        <p class="text-xl text-zinc-400 mb-10 max-w-lg mx-auto">
            Premium accounts, high OVR players, millions of coins. Safe marketplace for FC Mobile enthusiasts.
        </p>
        <div class="flex gap-4 justify-center">
            <a href="shop.php" class="px-10 py-4 bg-green-600 hover:bg-green-500 rounded-2xl text-lg font-semibold flex items-center gap-3">
                Browse Shop <i class="fas fa-arrow-right"></i>
            </a>
            <a href="#" onclick="document.getElementById('sell-modal').classList.remove('hidden')" 
               class="px-10 py-4 border border-zinc-700 hover:bg-zinc-900 rounded-2xl text-lg font-semibold">
                Sell Your ID
            </a>
        </div>
    </div>
</section>

<!-- Categories -->
<section class="max-w-7xl mx-auto px-6 py-16">
    <h2 class="text-3xl font-bold mb-8">Categories</h2>
    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
        <a href="shop.php?cat=high_ovr" class="bg-zinc-900 hover:bg-zinc-800 p-6 rounded-3xl text-center transition-all card-hover">
            <div class="text-5xl mb-4">⭐</div>
            <h3 class="font-semibold">High OVR</h3>
        </a>
        <a href="shop.php?cat=coins" class="bg-zinc-900 hover:bg-zinc-800 p-6 rounded-3xl text-center transition-all card-hover">
            <div class="text-5xl mb-4">💰</div>
            <h3 class="font-semibold">Coin Loaded</h3>
        </a>
        <a href="shop.php?cat=toty" class="bg-zinc-900 hover:bg-zinc-800 p-6 rounded-3xl text-center transition-all card-hover">
            <div class="text-5xl mb-4">🏆</div>
            <h3 class="font-semibold">TOTY / Icons</h3>
        </a>
        <!-- Add more categories -->
    </div>
</section>

<!-- Featured Products -->
<section class="max-w-7xl mx-auto px-6 py-16 bg-zinc-900">
    <div class="flex justify-between items-end mb-8">
        <h2 class="text-3xl font-bold">Featured IDs</h2>
        <a href="shop.php" class="text-green-500 hover:underline flex items-center gap-2">View All <i class="fas fa-arrow-right"></i></a>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6" id="featured-products">
        <!-- Populated by JS or PHP -->
    </div>
</section>

<!-- Footer -->
<footer class="bg-black py-12 border-t border-zinc-800">
    <div class="max-w-7xl mx-auto px-6 text-center text-zinc-500 text-sm">
        © 2026 FC ID Hub • Bangladesh • All Rights Reserved
    </div>
</footer>

<!-- Sell Modal -->
<div id="sell-modal" class="hidden fixed inset-0 bg-black/80 flex items-center justify-center z-50">
    <div class="bg-zinc-900 rounded-3xl max-w-lg w-full mx-4 p-8">
        <h3 class="text-2xl font-bold mb-6">Sell Your FC ID</h3>
        <form action="sell.php" method="POST" enctype="multipart/form-data">
            <input type="text" name="title" placeholder="ID Title" class="w-full bg-zinc-800 p-4 rounded-2xl mb-4" required>
            <textarea name="description" placeholder="Description (OVR, Coins, Players...)" class="w-full bg-zinc-800 p-4 rounded-2xl mb-4 h-32" required></textarea>
            <input type="number" name="price" placeholder="Price in BDT" class="w-full bg-zinc-800 p-4 rounded-2xl mb-4" required>
            <input type="file" name="image" class="w-full bg-zinc-800 p-4 rounded-2xl mb-6">
            <div class="flex gap-3">
                <button type="button" onclick="document.getElementById('sell-modal').classList.add('hidden')" 
                        class="flex-1 py-4 border border-zinc-700 rounded-2xl">Cancel</button>
                <button type="submit" class="flex-1 py-4 bg-green-600 rounded-2xl">List ID</button>
            </div>
        </form>
    </div>
</div>

<script>
// Tailwind script already included via CDN
function toggleTheme() {
    document.documentElement.classList.toggle('dark');
    const icon = document.getElementById('theme-icon');
    icon.classList.toggle('fa-moon');
    icon.classList.toggle('fa-sun');
}

// Simple cart counter (demo)
let cartCount = 0;
function addToCart() {
    cartCount++;
    document.getElementById('cart-count').textContent = cartCount;
}

// Demo products
const demoProducts = [
    {id:1, title:"117 OVR Account", price:4500, ovr:117, coins:"2.2B"},
    {id:2, title:"TOTY Mbappe ID", price:12500, ovr:115, coins:"980M"},
    {id:3, title:"High Coins Low OVR", price:1800, ovr:98, coins:"450M"},
];

function renderFeatured() {
    const container = document.getElementById('featured-products');
    container.innerHTML = demoProducts.map(p => `
        <div class="bg-zinc-900 rounded-3xl overflow-hidden card-hover">
            <div class="h-48 bg-gradient-to-br from-green-900 to-zinc-800 flex items-center justify-center text-6xl">
                ⚽
            </div>
            <div class="p-6">
                <div class="flex justify-between items-start">
                    <div>
                        <h4 class="font-semibold text-lg">${p.title}</h4>
                        <p class="text-green-400">${p.ovr} OVR • ${p.coins} Coins</p>
                    </div>
                    <div class="text-right">
                        <span class="text-2xl font-bold">৳${p.price}</span>
                    </div>
                </div>
                <button onclick="addToCart()" 
                        class="mt-6 w-full py-3.5 bg-green-600 hover:bg-green-500 rounded-2xl text-sm font-medium">
                    Add to Cart
                </button>
            </div>
        </div>
    `).join('');
}

window.onload = renderFeatured;
</script>
</body>
</html>
